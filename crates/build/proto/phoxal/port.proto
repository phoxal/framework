syntax = "proto3";

package phoxal.port;

import "google/protobuf/descriptor.proto";

enum PortKind {
  PORT_KIND_UNSPECIFIED = 0;
  STATE = 1;
  SAMPLE = 2;
  EVENT = 3;
  STREAM = 4;
  SETPOINT = 5;
  READ = 6;
  COMMANDS = 7;
}

extend google.protobuf.MethodOptions {
  PortKind kind = 50000;
}
