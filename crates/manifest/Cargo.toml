[package]
name = "phoxal-manifest"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = true
description = "Phoxal authored manifest readers and project compiler."
documentation.workspace = true
homepage.workspace = true
repository.workspace = true
readme = "README.md"

[lib]
name = "phoxal_manifest"
path = "src/lib.rs"

[dependencies]
phoxal-model = { workspace = true, features = ["schemars"] }
phoxal-runtime-contract = { workspace = true }
schemars = { workspace = true }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
serde_yaml = { workspace = true }
thiserror = { workspace = true }
toml = { workspace = true }
urdf-rs = { workspace = true }

[lints]
workspace = true

[dev-dependencies]
# Only the tests reach for a catch-all error type; every public entry point in
# this crate returns a typed one.
anyhow = { workspace = true }
jsonschema = { workspace = true, default-features = false }
tempfile = { workspace = true }
