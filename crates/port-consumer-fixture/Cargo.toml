[package]
name = "phoxal-port-consumer-fixture"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Isolated SDK port-feature consumer fixture."

[[bin]]
name = "phoxal-port-consumer-fixture"
path = "src/main.rs"

[dependencies]
phoxal = { workspace = true, default-features = false, features = ["port"] }

[lints]
workspace = true
