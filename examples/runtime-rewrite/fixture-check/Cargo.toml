[package]
name = "example-mujoco-fixture-check"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
license = "AGPL-3.0-only"
description = "Authoring-shape checks for the maintained MuJoCo fixture."

[dependencies]
phoxal = { path = "../../../phoxal", version = "=0.0.0-dev.2", default-features = false, features = ["artifact"] }
serde_yaml = "0.9"

[lints]
workspace = true
