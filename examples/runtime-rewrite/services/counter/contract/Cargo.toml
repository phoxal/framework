[package]
name = "example-counter-contract"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
license = "AGPL-3.0-only"
description = "The canonical generated contract for the Runtime example counter."

[lib]
name = "example_counter_contract"
path = "src/lib.rs"

[dependencies]
phoxal = { path = "../../../../../phoxal", version = "=0.0.0-dev.1", default-features = false, features = ["port"] }
prost = "0.14.4"

[build-dependencies]
phoxal = { path = "../../../../../phoxal", version = "=0.0.0-dev.1", default-features = false, features = ["build"] }

[lints]
workspace = true
