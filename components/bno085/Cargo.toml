[package]
name = "phoxal-component-bno085"
version = "0.68.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = ["phoxal"]
description = "Phoxal official driver: bno085 - IMU."
documentation.workspace = true
homepage.workspace = true
repository.workspace = true
include = ["build.rs", "src/**", "proto/**", "Cargo.toml", "component.yaml", "model.xml"]

[package.metadata.phoxal]
kind = "component"

[lib]
name = "phoxal_component_bno085"
path = "src/lib.rs"

[[bin]]
name = "phoxal-component-bno085"
path = "src/main.rs"
required-features = ["runtime"]

[features]
default = ["runtime"]
runtime = ["dep:phoxal", "dep:anyhow", "dep:serde"]

[dependencies]
phoxal = { workspace = true, features = ["runtime"], optional = true }
anyhow = { workspace = true, optional = true }
phoxal-port = { workspace = true }
prost = { workspace = true }
serde = { workspace = true, optional = true }

[build-dependencies]
phoxal-build = { workspace = true }

[lints]
workspace = true
