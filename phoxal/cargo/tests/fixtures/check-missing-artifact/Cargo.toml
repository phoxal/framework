[workspace]

[package]
name = "check-missing-robot"
version = "0.1.0"
edition = "2024"
build = "build.rs"
publish = false

[dependencies]
phoxal-supervisor = { version = "*", registry = "phoxal" }

[patch.phoxal]
phoxal-supervisor = { path = "supervisor" }
