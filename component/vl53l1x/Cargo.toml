[package]
name = "phoxal-component-vl53l1x"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = ["phoxal"]
description = "Phoxal official driver: vl53l1x - range/time-of-flight."
documentation.workspace = true
homepage.workspace = true
repository.workspace = true

[[bin]]
name = "phoxal-component-vl53l1x"
path = "src/main.rs"

[dependencies]
phoxal = { workspace = true }
anyhow = { workspace = true }

[dev-dependencies]
# Tests use only the explicit caller-owned harness; shipped binaries do not
# enable this bypass feature.
phoxal = { workspace = true, features = ["test-harness"] }

phoxal-bus = { workspace = true }
tokio = { workspace = true, features = ["macros", "rt-multi-thread", "time"] }

[lints]
workspace = true
