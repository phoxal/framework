[package]
name = "phoxal-test-robot"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false
license.workspace = true
description = "Internal four-wheel native-motion qualification robot."

[dependencies]
phoxal = { workspace = true, features = ["runtime"] }
phoxal-supervisor = { path = "../../supervisor", version = "=0.0.0-dev.3" }
serde_json.workspace = true

[build-dependencies]
phoxal = { workspace = true, default-features = false, features = ["build"] }

[lints]
workspace = true
