# phoxal-kinematics

Generated messages and typed public ports for the official kinematics service.

Kinematics owns measured joint state, continuous differential-drive odometry, model-backed frame transforms, bounded transform history, and explicit unavailable status.

The contract contains no runtime, transport, hardware, model parser, or simulator dependency.
